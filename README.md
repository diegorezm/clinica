# Setup

Create docker db:

```sh
docker-compose -f docker-compose.dev.yml up -d

```

To install dependencies:

```sh
bun install
```

To run:

```sh
bun run dev
```

# Todos

## Services:

    - [] Create updates
